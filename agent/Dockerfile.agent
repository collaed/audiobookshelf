FROM python:3.12-slim
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists/*
WORKDIR /agent
COPY abs-wrapper.py abs-agent.py ./
ENTRYPOINT ["python3", "abs-wrapper.py"]
CMD ["--server", "http://audiobookshelf:80"]
